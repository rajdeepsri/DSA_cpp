#include<bits/stdc++.h>
using namespace std;

class node{
    public:
    int data;
    node* left;
    node* right;
    node(int d){
        this->data=d;
        this->left=nullptr;
        this->right=nullptr;
    }
};

node* buildTree(){
    cout<<"enter the data"<<endl;
    int data;
    cin>>data;
    root = 
}

int main(){
    node* root = nullptr;

    //creating tree
    root = buildTree(root);
    return 0;
}